# Core dependencies
torch>=2.0.0
numpy>=1.24.0
gym>=0.26.0
gymnasium>=0.28.0
# MuJoCo (explicit version to avoid compatibility issues)
mujoco==3.1.6
# Meta-RL and Multi-agent
# MetaWorld: avoiding yanked 2.0.0 version, use git repo for stable version
git+https://github.com/Farama-Foundation/Metaworld.git
pettingzoo>=1.24.0
# Deep RL libraries
stable-baselines3>=2.0.0
# torch-rl>=0.1.0  # Commented out due to compatibility issues
# SSM (State Space Models)
mamba-ssm>=1.0.0
einops>=0.7.0
# Utilities
matplotlib>=3.7.0
seaborn>=0.12.0
tensorboard>=2.13.0
pyyaml>=6.0
tqdm>=4.65.0
scipy>=1.10.0
pandas>=2.0.0
# Logging
wandb>=0.17.0
# Testing
pytest>=7.3.0
